dependencies:
  lvgl/lvgl:
    version: "^8.3.7"
    rules:
      - if: "target == esp32s3"

  espressif/esp_lcd_touch_gt911:
    version: "==1.0.7~1"
    rules:
      - if: "target == esp32s3"

  idf:
    version: ">=4.1.0"
